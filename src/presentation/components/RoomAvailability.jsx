import { Groups2Outlined, LocationOnOutlined, VideocamOutlined, TvOutlined } from "@mui/icons-material";
import styles from "./RoomAvailability.module.css";

export function RoomAvailability({ rooms }) {
  return <section className={styles.panel}><div className={styles.head}><div><h3>Oda uygunluğu</h3><p>Şu anki kullanılabilirlik durumu</p></div><button className={styles.textButton}>Tüm odalar</button></div><div className={styles.list}>{rooms.map((room) => <article className={styles.item} key={room.id}><span className={`${styles.symbol} ${room.available ? "" : styles.busySymbol}`}><span>{room.name.slice(0,2).toUpperCase()}</span></span><div className={styles.copy}><div><h4>{room.name}</h4><span className={`${styles.state} ${room.available ? styles.available : styles.busy}`}><i />{room.available ? "Müsait" : "Dolu"}</span></div><p><LocationOnOutlined />{room.location}<span>·</span><Groups2Outlined />{room.capacity} kişi</p><div className={styles.features}>{room.features.map((feature) => <small key={feature}>{feature.includes("Video") ? <VideocamOutlined /> : <TvOutlined />}{feature}</small>)}</div></div><button disabled={!room.available}>{room.available ? "Rezerve et" : "14:30'da boş"}</button></article>)}</div></section>;
}
